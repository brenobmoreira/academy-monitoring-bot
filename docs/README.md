# docs

- `specs/` — approved designs, one file per feature (`YYYY-MM-DD-<topic>-design.md`).
- `plans/` — implementation plans derived from a spec.
- `adr/` — architecture decision records (`NNNN-<slug>.md`).

App-specific setup guides live next to the app: `apps/<name>/docs/setup.md`.
